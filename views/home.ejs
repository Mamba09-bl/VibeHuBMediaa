<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white min-h-screen">
    <nav class="w-full bg-gray-900 text-white shadow-lg mb-8">
      <div
        class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between"
      >
        <h1 class="text-2xl font-bold text-blue-400">VibeHubMedia</h1>

        <div class="flex-grow flex justify-center space-x-8">
          <a href="/home" class="flex flex-col items-center">
            <svg
              class="w-6 h-6 text-blue-400"
              fill="currentColor"
              viewBox="0 0 20 20"
            >
              <path
                d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"
              ></path>
            </svg>
          </a>
          <a href="/profile" class="flex flex-col items-center">
            <svg
              class="w-6 h-6 text-gray-500 hover:text-blue-400 transition"
              fill="currentColor"
              viewBox="0 0 20 20"
            >
              <path
                d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z"
                clip-rule="evenodd"
                fill-rule="evenodd"
              ></path>
            </svg>
          </a>
        </div>

        <div class="w-[150px] md:w-[150px]"></div>
      </div>
    </nav>

    <main class="max-w-6xl mx-auto px-4 space-y-12 pb-12">
      <section>
        <h2
          class="text-2xl font-bold text-blue-400 mb-6 border-b-2 border-blue-400 inline-block pb-1"
        >
          Your Posts 🚀
        </h2>
        <% if (posts.length > 0) { %>
        <div class="space-y-8">
          <% user.forEach(post => { %>
          <div
            class="bg-gray-800 rounded-lg shadow-xl p-6 border border-gray-700"
          >
            <div class="flex items-center mb-4">
              <% if (showpost.image) { %>
              <img
                src="/images/uploads/<%= showpost.image %>"
                alt="Profile Picture"
                class="w-10 h-10 rounded-full object-cover mr-3"
              />
              <% } else { %>
              <div
                class="w-10 h-10 rounded-full bg-blue-500 flex items-center justify-center text-white font-bold text-sm mr-3"
              >
                <%= posts.username ? posts.username[0].toUpperCase() : 'U' %>
              </div>
              <% } %>
              <div>
                <p class="font-semibold text-gray-200">
                  <%= posts.username || posts.email %>
                </p>
                <p class="text-sm text-gray-400">
                  <%= new Date(post.uploadTime).toLocaleString() %>
                </p>
              </div>
            </div>

            <p class="text-gray-200 mb-4 leading-relaxed">
              <%= post.postText %>
            </p>
            <% if (post.image) { %>
            <img
              src="/images/uploads/<%= post.image %>"
              alt="post image"
              class="rounded-lg mx-auto max-w-md object-contain shadow-md border border-gray-700"
            />
            <% } %>
          </div>
          <% }) %>
        </div>
        <% } else { %>
        <p class="text-gray-500 italic">
          You haven't posted anything yet. Start sharing your vibe! ✨
        </p>
        <% } %>
      </section>

      <section>
        <h2
          class="text-2xl font-bold text-purple-400 mb-6 border-b-2 border-purple-400 inline-block pb-1"
        >
          People You Follow 🫂
        </h2>
        <% if (following.length > 0) { %>
        <div class="space-y-10">
          <% following.forEach(f => { %>
          <div
            class="bg-gray-800 rounded-2xl shadow-xl p-8 border border-gray-700"
          >
            <div class="flex items-center mb-6">
              <% if (f.image) { %>
              <img
                src="/images/uploads/<%= f.image %>"
                alt="<%= f.username %>"
                class="w-16 h-16 rounded-full border-3 border-purple-500 mr-4 object-cover shadow-md"
              />
              <% } else { %>
              <div
                class="w-16 h-16 rounded-full bg-purple-600 flex items-center justify-center text-white text-xl font-bold mr-4"
              >
                <%= f.username ? f.username[0].toUpperCase() : "U" %>
              </div>
              <% } %>
              <div>
                <p class="font-bold text-xl text-gray-100">
                  <%= f.username || f.email %>
                </p>
                <p class="text-sm text-gray-400"><%= f.email %></p>
              </div>
            </div>

            <% if (f.post.length > 0) { %>
            <div class="space-y-6">
              <% f.post.forEach(p => { %>
              <div
                class="border border-gray-700 rounded-xl p-5 bg-gray-700 hover:shadow-lg transition transform duration-300"
              >
                <div class="flex items-center mb-3">
                  <% if (f.image) { %>
                  <img
                    src="/images/uploads/<%= f.image %>"
                    alt="<%= f.username %>"
                    class="w-8 h-8 rounded-full object-cover mr-3"
                  />
                  <% } else { %>
                  <div
                    class="w-8 h-8 rounded-full bg-purple-500 flex items-center justify-center text-white font-bold text-xs mr-3"
                  >
                    <%= f.username ? f.username[0].toUpperCase() : 'U' %>
                  </div>
                  <% } %>
                  <div>
                    <p class="font-semibold text-gray-200 text-sm">
                      <%= f.username || f.email %>
                    </p>
                    <p class="text-xs text-gray-400">
                      <%= new Date(p.uploadTime).toLocaleString() %>
                    </p>
                  </div>
                </div>

                <p class="text-gray-300 mb-3"><%= p.postText %></p>

                <% if (p.image) { %>
                <img
                  src="/images/uploads/<%= p.image %>"
                  alt="post image"
                  class="mt-3 rounded-lg mx-auto max-w-xs object-contain shadow-sm border border-gray-600"
                />
                <% } %>
              </div>
              <% }) %>
            </div>
            <% } else { %>
            <p class="text-gray-500 italic">
              No posts yet from <%= f.username || f.email %>. 😔
            </p>
            <% } %>
          </div>
          <% }) %>
        </div>
        <% } else { %>
        <p class="text-gray-500 italic">
          You aren't following anyone yet. Find some friends! 👀
        </p>
        <% } %>
      </section>
    </main>
  </body>
  <script>
    function openImageModal(src) {
      document.getElementById("modalImage").src = src;
      document.getElementById("imageModal").classList.remove("hidden");
      document.getElementById("imageModal").classList.add("flex");
    }

    function closeImageModal() {
      document.getElementById("imageModal").classList.add("hidden");
      document.getElementById("imageModal").classList.remove("flex");
    }
  </script>
  <script src="https://cdn.socket.io/4.8.1/socket.io.min.js"></script>
  <script src="/js/comment.js"></script>
</html>
